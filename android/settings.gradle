// This is the main settings file for your project.

include ':app'

// Directly include capacitor-android and set its path
include ':capacitor-android'
project(':capacitor-android').projectDir = new File('../node_modules/@capacitor/android/capacitor')

// Removed the line that applied capacitor.settings.gradle to avoid including cordova-android-plugins
// apply from: 'capacitor.settings.gradle'
