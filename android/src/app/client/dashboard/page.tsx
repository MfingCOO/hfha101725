'use client';
import { DashboardClient } from '@/components/dashboard/dashboard-client';

// Note: This component is now a pass-through from the layout.
// The layout will provide the necessary props.
export default function ClientDashboardPage() {
  return (
    <DashboardClient />
  )
}
